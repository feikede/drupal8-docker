# Some approaches to have drupal 8 running in Docker

## Problems

* mail doesnt't work from inside the container
* need a shared filesystem for sites/default/files
* need a db link
* need a redis cach backend


